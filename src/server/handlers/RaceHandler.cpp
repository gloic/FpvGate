#include "RaceHandler.h"

void RaceHandler::begin() {
    /*
        From a TRACK:
            send listen to Starter (stop)
            once passed, send listen to next gate etc...
            once finished, restart
            
        when reset, restart track
    */
}

void RaceHandler::end() {
    // send listen/stop to all
}