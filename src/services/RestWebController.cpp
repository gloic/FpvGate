#include "headers/RestWebController.h"

#include <Arduino.h>

// RestWebController::RestWebController() {}

void RestWebController::setup() { }

void RestWebController::start() {
    
}

void RestWebController::notifyPass() {
    // Serial.println("Notify pass to Starter");
    // char url[100];
    // snprintf(url, sizeof(url), "http://%s/api/gate/passed", ipStarter.c_str());
    
    // http.begin(wifiClient, url);
    // http.addHeader("Content-Type", "application/x-www-form-urlencoded");

    // String payload = "id=" + id;
    // http.POST(payload);
    // http.end();
}